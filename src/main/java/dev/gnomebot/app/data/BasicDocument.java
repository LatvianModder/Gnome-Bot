package dev.gnomebot.app.data;

import dev.gnomebot.app.util.MapWrapper;

/**
 * @author LatvianModder
 */
public class BasicDocument extends WrappedDocument<BasicDocument> {
	public BasicDocument(WrappedCollection<BasicDocument> c, MapWrapper d) {
		super(c, d);
	}
}